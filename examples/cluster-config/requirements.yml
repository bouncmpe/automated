# ansible galaxy requirements
#
# See: https://docs.ansible.com/ansible/latest/galaxy/user_guide.html
# Use: ansible-galaxy install -r requirements.yml --force

collections:
  - source: git+https://github.com/bouncmpe/automated.git
    type: git
